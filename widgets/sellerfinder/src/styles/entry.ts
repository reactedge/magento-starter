import tokens from "./widget-tokens.css?inline";
import map from "./seller-finder.map.css?inline";
import card from "./seller-finder.card.css?inline";
import table from "./seller-finder.table.css?inline";
import search from "./seller-finder.search.css?inline"

export const styles = [
    tokens,   // MUST be first
    map,
    card,
    table,
    search
];