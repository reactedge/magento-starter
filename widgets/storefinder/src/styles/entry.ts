import tokens from "./widget-tokens.css?inline";
import map from "./store-finder.map.css?inline";
import card from "./store-finder.card.css?inline";
import table from "./store-finder.table.css?inline";
import search from "./store-finder.search.css?inline"

export const styles = [
    tokens,   // MUST be first
    map,
    card,
    table,
    search
];